Small status or metadata pill — payment status, trial flags, counts.

```jsx
<Badge tone="success" dot>Active</Badge>
<Badge tone="warning">Trial ends soon</Badge>
<Badge tone="danger" solid>Due today</Badge>
```

- Soft (tinted) is the default and most common. Use `solid` only for urgent states.
- Keep labels to 1–2 words.
