Icon-only tap target for toolbars, nav bars and row actions. Always pass a `label` for accessibility.

```jsx
<IconButton label="Notifications"><Icon name="bell" /></IconButton>
<IconButton variant="primary" shape="round" label="Add"><Icon name="plus" /></IconButton>
```

- Min 40px (44px in primary mobile contexts) to meet the iOS tap target.
