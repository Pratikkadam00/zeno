Binary on/off control for settings and notification preferences.

```jsx
<Switch checked={reminders} onChange={setReminders} />
```

- Controlled: pass `checked` and handle `onChange(next)`. Green = on. The knob springs.
