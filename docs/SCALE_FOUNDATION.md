# Scale Foundation

## Implemented Contracts

- Widget and watch snapshots expose compact renewal, spend, and complication data.
- Business tier contracts model workspaces, seats, team spend, and renewal load.
- Public API key previews mask key material and enforce explicit scopes.
- Partner manifests describe scopes, status, and financial-export behavior.

## Native Follow-Up

- Add iOS widget extension and Android widget provider once native projects are generated.
- Add Watch complication extension after iOS dev-build validation.
- Persist widget snapshots in a native-accessible encrypted/shared location approved by platform constraints.

## API Follow-Up

- Replace demo public API keys with secure creation flow.
- Add webhook signing and replay protection.
- Add partner OAuth flows only after credentials and provider reviews are complete.
